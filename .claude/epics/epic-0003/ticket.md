# Implement OAuth Authentication for Anthropic API

Implement OAuth authentication for Anthropic API like OpenCode does, replacing the current simple API key approach with OAuth flow (access tokens, refresh tokens, expiry handling)
