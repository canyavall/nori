import type { Component } from 'solid-js';

export const GitBadge: Component = () => (
  <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded text-xs bg-[var(--color-bg-tertiary)] text-[var(--color-text-muted)]">
    <span>git</span>
  </span>
);
