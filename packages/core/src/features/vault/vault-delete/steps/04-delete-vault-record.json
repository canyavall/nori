{
  "type": "action",
  "what": "Read vault data then delete the vault row from the database",
  "why": "The vault row must be removed from the database. Vault data is read first so it can be included in the response.",
  "where": {
    "entry_point": "features/vault/vault-delete/vault-delete.ts",
    "implementation": "features/vault/vault-delete/actions/delete-vault-record.ts"
  },
  "output": { "console": false, "file": false },
  "success_handling": {
    "criteria": "Vault row deleted from database",
    "event": { "action": "vault:delete:deleting-vault-record", "status": "success", "message": "Vault record deleted", "data_fields": ["vault_id"] }
  },
  "error_handling": [
    {
      "scenario": "Database error during deletion",
      "criteria": "SQL error thrown",
      "action": "Return DB_DELETE_FAILED error",
      "severity": "fatal",
      "recoverable": false
    }
  ],
  "decisions": [
    { "date": "2026-03-03", "reason": "Read vault before deleting", "rationale": "Need vault_name, vault_type, local_path for response and conditional file deletion in step 05" }
  ],
  "calls": ["features/vault/vault-delete/actions/delete-vault-record.ts"]
}
