{
  "type": "action",
  "what": "Confirm the vault exists in the database",
  "why": "Cannot unlink a project from a vault that doesn't exist",
  "where": {
    "entry_point": "features/vault/vault-unlink-project/vault-unlink-project.ts",
    "implementation": "features/vault/vault-link-project/actions/validate-vault-exists.ts"
  },
  "output": { "console": false, "file": false },
  "success_handling": {
    "criteria": "Vault row found in database",
    "event": { "action": "vault:unlink-project:validating-vault", "status": "success", "message": "Vault verified", "data_fields": ["vault_id"] }
  },
  "error_handling": [
    {
      "scenario": "Vault not found",
      "criteria": "No row returned for vault_id",
      "action": "Return VAULT_NOT_FOUND error",
      "severity": "error",
      "recoverable": true
    }
  ],
  "decisions": [
    { "date": "2026-02-22", "reason": "Reuse validate-vault-exists from vault-link-project", "rationale": "Identical logic — check vault exists and return its row" }
  ],
  "calls": ["features/vault/vault-link-project/actions/validate-vault-exists.ts"]
}
